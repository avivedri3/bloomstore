# Override in the shell or CI when the API is not on this machine.
# An empty value falls back to http://localhost:3030/api in the client.
VITE_API_BASE_URL=http://localhost:3030/api
VITE_BASE_URL=/bloomstore/
