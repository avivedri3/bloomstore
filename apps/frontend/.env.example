# Set VITE_API_BASE_URL to the API the SPA should call (include /api).
# When it is unset, the client uses http://localhost:3030/api.
VITE_API_BASE_URL=http://localhost:3030/api
VITE_BASE_URL=/bloomstore/
